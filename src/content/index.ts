/** Content barrel — single import surface for prototype content. */
export * from "@/content/types";
export { primaryNav, secondaryNav, donateNav, organization, contact } from "@/content/data/site";
